# Core runtime values for the GoodWe dashboard.
#
# Sign conventions:
# - pbattery1: positive = discharging to house/grid, negative = charging.
# - active_power: positive = exporting to grid, negative = importing from grid.

battery_soc
ppv
pbattery1
house_consumption
active_power

# ET/EH extended MPPT alternative for solar watts, if your model exposes it:
#ppv_total

# Useful optional extras:
# battery_mode_label
# grid_in_out_label
# e_day
# e_total
# e_bat_charge_total
# e_bat_discharge_total
# meter_e_total_imp
# meter_e_total_exp
# temperature
#
# ES/EM fallback if active_power is unavailable:
# pgrid
